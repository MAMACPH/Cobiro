<section class="section">
	<div class="container">
		<div class="row">
			<div class="col">
				<div class="bg-lightgrey flex testimonal-box middle-md">
					<div class="testimonial-image-wrapper">
						<img class="testimonial-image" src="{{ image.url }}" alt="{{ image.title }}"/>
					</div>
					<div>
						<h4>“{{ quote }}”</h4>
						<p class="small">- {{ person }}</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>
