{% if backgroundColor %}
	{% set style = 'style=background-color:' ~ backgroundColor %}
{% else %}
	{% set style = null %}
{% endif %}
{% if idValue %}
	{% set id = 'id=' ~ idValue %}
{% else %}
	{% set id = null %}
{% endif %}
<section class="section" {{ style }} {{ id }}>
	<div class="container">
		<div class="row middle-xs">
			<div class="col col-sm-12 col-md-6">
				<div class="img-responsive space-sm space-xs">
						{% import 'macros/transformed-image' as transformedImage %}
						{{ transformedImage.image(image, image.title, "", [
							{ width: 1800 },
							{ width: 1500 },
							{ width: 1200 },
							{ width: 900 },
							{ width: 600, jpegQuality: 65 },
							{ width: 400, jpegQuality: 65 },
							{ width: 300, jpegQuality: 65 }], "(max-width: 679px) 96vw, (max-width: 1200px) 45vw, 600px" ) }}
				</div>
			</div>
			<div class="col col-sm-12 col-md-6">
				<div class="text-padding">
					<h3 class="">{{ heading }}</h3>
					{{ text }}
					{% if explanationRightLink %}
						{% if explanationRightLink.href.one.internalHref.one.url() is defined %}
						<a href="{{ explanationRightLink.href.one.internalHref.one.url() }}{{ explanationRightLink.href.one.idLink ?  '#' ~ explanationRightLink.href.one.idLink : ''}}">{{ explanationRightLink.href.one.linkTitle }}</a>
						{% elseif explanationRightLink.href.one.externalHref is defined %}
						<a href="{{ explanationRightLink.href.one.externalHref }}" target="_blank">{{ explanationRightLink.href.one.linkTitle }}</a>
						{% endif %}
					{% endif %}
				</div>
			</div>
		</div>
	</div>
</section>
