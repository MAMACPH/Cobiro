{% cache %}
{% set entry = entry %}
{% set pageContent = { title : entry.title } %}
{% include 'includes/partials/header.twig' with pageContent %}
{% set blocks = entry.sections.with(
	['jumboHeader.children.children.children',
	'featuredTestimonalSingle.children.children.children',
	'threeUp.children.children.children',
	'featuredCompanies.children.children.children',
	'explanationLeft.children.children.children',
	'explanationRight.children.children.children',
	'jumboCta.children.children.children',
	'threeUpPeople.children.children.children',
	'accordionFaq.children.children.children',
	'textEditor.children.children.children',
	'listPricing.children.children.children',
	'video.children.children.children',
	'quotes.children.children.children',
	'featureList.children.children.children',]).all()
	%}

{% for block in blocks %}
	{% if block.type == "jumboHeader" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			jumboHeaderLink : block.jumboHeaderLink.one(),
			image : block.image.one(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/jumbo-header.twig' with blockContent %}
	{% elseif block.type == "featuredTestimonalSingle" %}
		{% set blockContent = {
			image : block.image.one(),
			quote : block.quote,
			person : block.person,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/featured-testimonal-single.twig' with blockContent %}
	{% elseif block.type == "threeUp" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			cards : block.cards.all(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/3-up.twig' with blockContent %}
	{% elseif block.type == "featuredCompanies" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			images : block.images.all(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/featured-companies.twig' with blockContent %}
	{% elseif block.type == "explanationLeft" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			explanationLeftLink : block.explanationLeftLink.one(),
			image : block.image.one(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/explanation-left.twig' with blockContent %}
	{% elseif block.type == "explanationRight" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			explanationRightLink : block.explanationRightLink.one(),
			image : block.image.one(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/explanation-right.twig' with blockContent %}
	{% elseif block.type == "jumboCta" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			jumboCtaLink : block.jumboCtaLink,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/jumbo-cta.twig' with blockContent %}
	{% elseif block.type == "threeUpPeople" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			persons : block.persons.all(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/3-up-people.twig' with blockContent %}
	{% elseif block.type == "accordionFaq" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			faqs : block.faqs,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/accordion-faq.twig' with blockContent %}
	{% elseif block.type == "textEditor" %}
		{% set blockContent = {
			text : block.text,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink } %}
	{% include 'includes/sections/text-editor.twig' with blockContent %}
	{% elseif block.type == "listPricing" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			pricings : block.pricings,
			baseFeatures : block.baseFeatures,
			premiumFeatures : block.premiumFeatures,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
	{% include 'includes/sections/list-pricing.twig' with blockContent %}
	{% elseif block.type == "video" %}
		{% set blockContent = {
			embedCode : block.embedCode,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
		{% include 'includes/sections/video.twig' with blockContent %}
	{% elseif block.type == "quotes" %}
		{% set blockContent = {
			heading : block.heading,
			text : block.text,
			quotes : block.quotes,
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
		{% include 'includes/sections/quotes.twig' with blockContent %}
	{% elseif block.type == "featureList" %}
		{% set blockContent = {
			featureList : block.features.all(),
			backgroundColor : block.backgroundColor,
			idValue : block.idLink
		} %}
		{% include 'includes/sections/feature-list.twig' with blockContent %}
	{% endif %}
{% endfor %}
{% include 'includes/partials/footer.twig' %}
{% endcache %}
